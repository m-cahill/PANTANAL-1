# M03 Kaggle Evidence — Baseline Notebook

**Status:** Not yet executed / awaiting owner manual Kaggle run.

**Authority:** Subordinate to `docs/pantanal-1.md`. Do not update claims in Ultimate Truth until observed values are recorded below.

Related: `notebooks/pantanal_1_m03_baseline.ipynb`, `docs/kaggle/baseline_inference_notebook.md`, `docs/kaggle/kaggle_submission_bible.md`.

---

## Notebook

- Source notebook in repo: `notebooks/pantanal_1_m03_baseline.ipynb`
- Kaggle notebook URL:
- Kaggle notebook version / commit:
- Date/time run:
- Runner:

---

## Mode

- Interactive:
- Commit:
- Submit:
- Notes:

---

## Runtime settings

- Accelerator:
- Internet:
- Observed `KAGGLE_KERNEL_RUN_TYPE`:
- Python version:
- Platform:
- cwd:

---

## Input discovery

- `/kaggle/input` exists:
- `/kaggle/input` children:
- `sample_submission.csv` discovered:
- Exact sample path:
- Discovery method:

---

## Output evidence

- Mode selected by notebook:
  - `REAL_SAMPLE_ZERO_BASELINE`
  - `SYNTHETIC_FALLBACK_ONLY`
- `/kaggle/working/submission.csv` produced:
- Synthetic fallback output produced:
- Output path:
- Row count:
- Column count:
- Header preview:
- File size bytes:

---

## Runtime / errors

- Runtime:
- Errors:
- Warnings:

---

## Submission / scoring

- Submitted to competition:
- Submission ID:
- Public leaderboard score:
- Private leaderboard score:
- Scoring still available:
- Notes:

---

## Claim recommendation

Only update `docs/pantanal-1.md` claims if direct evidence supports it.

**Potential outcomes:**

1. If `REAL_SAMPLE_ZERO_BASELINE` runs and `/kaggle/working/submission.csv` is produced in commit/submit mode, **DEF-002B** may be narrowed or closed depending on evidence.
2. If real `sample_submission.csv` is discovered and exact header/row order is preserved, **DEF-003** may be narrowed.
3. If only synthetic fallback runs, preserve **DEF-002B** and **DEF-003** as open.
4. If execution fails, record the blocker without overclaiming.

**Do not claim** leaderboard score, model inference, CPU 90-minute compliance, or active competition eligibility unless directly observed and documented above.
