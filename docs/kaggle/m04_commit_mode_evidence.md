# M04 Commit-Mode Evidence

**Status:** Not yet executed / awaiting owner manual Kaggle commit-mode run.

**Authority:** Subordinate to `docs/pantanal-1.md`. Do not treat this template as proof of commit/submit-mode success until filled with observed values.

Related: `docs/kaggle/m04_commit_mode_probe.md`, `docs/kaggle/m03_kaggle_evidence.md`.

---

## Status vocabulary

Allowed field values:

```text
yes
no
blocked — deadline passed
N/A — not attempted
```

Use **blocked — deadline passed** when Kaggle blocks commit/submit/scoring because the deadline passed. Use **N/A — not attempted** only when the step was not tried. Record the exact observed blocker if different.

---

## Notebook

- URL:
- Version / commit:
- Date/time:
- Runner:

---

## Mode

- Interactive:
- Commit:
- Submit:
- Notes:

---

## Runtime settings

- Accelerator:
- Internet:
- Runtime:
- `KAGGLE_KERNEL_RUN_TYPE`:

---

## Input discovery

- `/kaggle/input` children:
- `sample_submission.csv` discovered:
- Exact path:
- Row count (sample):
- Column count (sample):

---

## Output evidence

- `/kaggle/working/submission.csv` produced:
- File size:
- Row count:
- Column count:
- Header preview:
- First `row_id`:
- Last `row_id`:

---

## Submission / scoring

- Submit button available:
- Submitted:
- Submission ID:
- Scoring still available:
- Public leaderboard score:
- Private leaderboard score:
- Errors/warnings:

Example when deadline blocks platform (replace with observed values):

```text
Submit button available: blocked — deadline passed
Submitted: no
Scoring still available: blocked — deadline passed
```

---

## Deferred issue assessment

- **DEF-002B:**
- **DEF-003B:**

---

## Claim recommendation

Do not update `docs/pantanal-1.md` claims unless evidence above directly supports the change. Interactive-only or template-only state does not close DEF-002B or DEF-003B.
